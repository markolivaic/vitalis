/**
 * Vitalis AI | Health & Performance Hub
 * File: next.config.ts
 * Description: Next.js configuration file for build and runtime settings.
 */

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
